import { NextResponse } from 'next/server';

// Exemple d'API route pour le formulaire de contact
// Décommentez et configurez selon votre service email préféré

export async function POST(request: Request) {
  try {
    const { name, email, subject, message } = await request.json();

    // Validation basique
    if (!name || !email || !subject || !message) {
      return NextResponse.json(
        { error: 'Tous les champs sont requis' },
        { status: 400 }
      );
    }

    // Option 1: Avec Resend (recommandé)
    // const { Resend } = await import('resend');
    // const resend = new Resend(process.env.RESEND_API_KEY);
    // 
    // const data = await resend.emails.send({
    //   from: 'Portfolio <noreply@votre-domaine.com>',
    //   to: [process.env.CONTACT_EMAIL || 'hello@exemple.com'],
    //   subject: `[Portfolio] ${subject}`,
    //   html: `
    //     <h2>Nouveau message depuis votre portfolio</h2>
    //     <p><strong>Nom:</strong> ${name}</p>
    //     <p><strong>Email:</strong> ${email}</p>
    //     <p><strong>Sujet:</strong> ${subject}</p>
    //     <div>
    //       <strong>Message:</strong>
    //       <p>${message.replace(/\n/g, '<br>')}</p>
    //     </div>
    //   `,
    // });

    // Option 2: Avec Nodemailer
    // const nodemailer = await import('nodemailer');
    // 
    // const transporter = nodemailer.createTransporter({
    //   service: 'gmail', // ou votre service SMTP
    //   auth: {
    //     user: process.env.EMAIL_USER,
    //     pass: process.env.EMAIL_PASS,
    //   },
    // });
    // 
    // await transporter.sendMail({
    //   from: process.env.EMAIL_USER,
    //   to: process.env.CONTACT_EMAIL,
    //   subject: `[Portfolio] ${subject}`,
    //   html: `
    //     <h2>Nouveau message depuis votre portfolio</h2>
    //     <p><strong>Nom:</strong> ${name}</p>
    //     <p><strong>Email:</strong> ${email}</p>
    //     <p><strong>Sujet:</strong> ${subject}</p>
    //     <div>
    //       <strong>Message:</strong>
    //       <p>${message.replace(/\n/g, '<br>')}</p>
    //     </div>
    //   `,
    // });

    // Pour tester sans service email, loggez simplement les données
    console.log('Message reçu:', { name, email, subject, message });

    return NextResponse.json({ 
      success: true, 
      message: 'Message envoyé avec succès!' 
    });

  } catch (error) {
    console.error('Erreur envoi email:', error);
    return NextResponse.json(
      { error: 'Erreur lors de l\'envoi du message' },
      { status: 500 }
    );
  }
}